apiVersion: batch/v1
kind: Job
metadata:
  name: vault-plugin-secrets-test-REPLACE_JOB_POSTFIX
  namespace: REPLACE_NAMESPACE
spec:
  template:
    metadata:
      labels:
        app: vault-plugin-secrets-test-REPLACE_JOB_POSTFIX
    spec:
      restartPolicy: OnFailure
      containers:
      - name: vault-plugin-secrets-test
        image: REPLACE_DOCKER_REPO:REPLACE_IMAGETAG
        imagePullPolicy: Always
        securityContext:
          capabilities:
            add:
              - IPC_LOCK
        ports:
        - containerPort: 8200
          name: port8200
        volumeMounts:
        - mountPath: "/data/"
          name: data
        env:
        - name: VAULT_ADDR
          value: "http://127.0.0.1:8200"
        - name: VAULT_API_ADDR
          value: "http://127.0.0.1:8200"
        - name: UNSEAL
          value: "true"
      volumes:
      - name: data
        emptyDir: {}
  backoffLimit: 6
